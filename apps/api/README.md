# Syntrophos API

The API application owns transport concerns, request lifecycle management, and composition of backend packages. Milestone 1 exposes only `GET /health`; it intentionally contains no product, AI, vault, or memory behavior.

Copy `.env.example` to `.env` to override local server settings.
